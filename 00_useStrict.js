// 'use strict' makes it easier to avoid errors; can be seen as a debugging measure;

'use strict';

let hasDriversLicense = false
const passTest = true

if (passTest) hasDriverLicense = true
if (hasDriversLicense) console.log("I can drive!");